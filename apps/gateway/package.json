{
  "name": "@evelandhq/gateway",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --import=@evelandhq/platform-observability/register --env-file=../../.env src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@evelandhq/core": "workspace:*",
    "@evelandhq/db": "workspace:*",
    "@evelandhq/platform-observability": "workspace:*",
    "@hono/node-server": "^2.0.6",
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
