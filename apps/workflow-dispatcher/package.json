{
  "name": "@eveland/workflow-dispatcher",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --import=@eveland/platform-observability/register --env-file=../../.env src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@eveland/core": "workspace:*",
    "@eveland/db": "workspace:*",
    "@eveland/platform-observability": "workspace:*",
    "@eveland/workflow-world": "workspace:*",
    "@workflow/world": "5.0.0-beta.19",
    "graphile-worker": "0.16.6",
    "pg": "8.20.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "vitest": "^4.1.9"
  }
}
