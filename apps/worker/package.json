{
  "name": "@eveland/worker",
  "version": "0.0.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @eveland/sandbox-bwrap build && tsx watch --env-file=../../.env src/worker.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "smoke:docker-sandbox": "tsx src/integration/docker-sandbox-smoke.ts"
  },
  "dependencies": {
    "@eveland/core": "workspace:*",
    "@eveland/db": "workspace:*",
    "@eveland/agent-observer": "workspace:*",
    "@eveland/sandbox-bwrap": "workspace:*",
    "@workflow/world-postgres": "5.0.0-beta.25",
    "execa": "^9.6.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
