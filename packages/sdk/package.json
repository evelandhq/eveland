{
  "name": "eveland",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    "./auth": {
      "types": "./dist/auth.d.ts",
      "import": "./dist/auth.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --maxWorkers=2 --testTimeout=30000",
    "prepack": "pnpm build"
  },
  "peerDependencies": {
    "eve": "catalog:eve-peer"
  },
  "devDependencies": {
    "eve": "catalog:",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
