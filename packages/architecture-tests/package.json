{
  "name": "@eveland/architecture-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace-wide architecture ratchets: dependency direction, Store narrowing, import cycles, browser-safe core exports, and configuration coverage.",
  "type": "module",
  "scripts": {
    "test": "vitest run --maxWorkers=2 --testTimeout=30000",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@eveland/core": "workspace:*",
    "@types/node": "^26.0.1",
    "vitest": "^4.1.9"
  }
}
